# Standup, 14 July

Blocked on the warehouse schema — the `orders` table has two `created_at` columns and
nobody knows which one the finance report uses. Asked Priya.

Decided: we ship the margin fix behind a flag rather than waiting.
