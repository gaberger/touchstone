# Layer 1 -- pure domain. No dependencies, by design.
[package]
name = "touchstone-domain"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "touchstone: pure domain model. Zero dependencies"

[dependencies]
