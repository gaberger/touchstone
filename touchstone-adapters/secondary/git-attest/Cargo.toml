# Secondary adapter for VersionControl. Ports only.
[package]
name = "touchstone-git-attest"
version.workspace = true
edition.workspace = true

[dependencies]
touchstone-ports = { path = "../../../touchstone-ports" }

[dev-dependencies]
tempfile = "3"
