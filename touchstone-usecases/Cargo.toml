# Layer 3 -- usecases. Domain + ports only (plus external crates -- ARCHITECTURE.md rule 3).
[package]
name = "touchstone-usecases"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "touchstone: use-case layer"

[dependencies]
touchstone-domain = { path = "../touchstone-domain" }
touchstone-ports = { path = "../touchstone-ports" }
