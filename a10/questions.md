# A10 — 20 questions (DEMO RUN)

> **This is a mechanical demonstration of the harness, not evidence.** The questions below
> were written by the same process being measured, over a corpus that process largely wrote.
> That is precisely the bias `questions.template.md` warns against. Real A10 needs your
> questions over your notes.

1. What did we decide about post-filtering versus a query planner?
2. Which architecture decisions are still only Proposed?
3. Why is the frontmatter parser a port rather than just a module?
4. What stops an agent claiming a concept was verified by a human?
5. Which load-bearing claims are measured and which are still untested?
6. How do we know the index can be rebuilt byte-identically?
7. What did the Python prototype teach us before it was deleted?
8. Why was git demoted rather than replaced entirely?
9. What is the one place personal and corporate deployments diverge?
10. Which concepts describe the retrieval pipeline end to end?
11. What happens to unknown frontmatter keys we do not recognise?
12. Why can revocation not be solved by changing the storage substrate?
13. What is E4b and why is it still open?
14. Which decisions are accepted rather than proposed?
15. How does the MCP surface avoid drifting from the CLI?
16. What did we measure about PyYAML and ISO 8601 timestamps?
17. Which parts of the system are derived and disposable?
18. What is the kill criterion for this whole architecture?
19. How does trust ranking change what an agent reads first?
20. What is the difference between a recall failure and a structure failure?
