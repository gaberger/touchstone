# A10 — 20 questions

> **Authored by Claude, at the user's instruction, over a corpus it partly wrote.** The bias
> is stated rather than hidden: prior knowledge of this corpus inflates the scorer's ability to
> judge "answered acceptably". Mitigations: the bulk of the corpus (DECISIONS, PROTOTYPE,
> VERSION-CONTROL, RUST-PATH, ADR-2608010900..0960) predates this session, and the answer key
> below was written from the questions, before either arm was run.
>
> Both arms face identical questions, so the COMPARISON is meaningful even where the absolute
> score is not.

1. What did we decide about post-filtering versus building a query planner?
2. Which architecture decisions are still only proposed?
3. Why is the frontmatter parser a port instead of a plain module?
4. What stops an agent from claiming a human verified a concept?
5. Which load-bearing claims are measured, and which are still untested?
6. Why was git demoted rather than replaced outright?
7. What is the one place personal and corporate deployments differ?
8. What happens to frontmatter keys we do not recognise?
9. Why can revocation not be fixed by changing the storage substrate?
10. What is the kill criterion that would stop this whole project?
11. How does trust ranking change what an agent reads first?
12. What did we measure about PyYAML and ISO 8601 timestamps?
13. Which parts of the system are derived and disposable?
14. What is the difference between a recall failure and a structure failure?
15. Why did the 50k index take seventeen minutes?
16. How does the MCP surface avoid drifting away from the CLI?
17. What is the reserved-filename bug that dropped a legitimate concept?
18. Why do we run drills against third-party bundles instead of only our own?
19. What did the Python prototype provide that was lost when it was deleted?
20. When is a broken link a problem and when is it normal?
